namespace SandboxService.Core.Exceptions;

public enum SandboxExceptionType
{
    ENTITY_NOT_FOUND,
    INVALID_KEYS,
    INSUFFICIENT_FUNDS,
    INVALID_ASSET,
    CURRENCY_NOT_FOUND,
    WALLET_DOES_NOT_EXIST,
    INVALID_OPERATION,
    POSITION_NOT_FOUND,
}

namespace SandboxService.Core.Exceptions;

public enum SandboxExceptionType
{
    RECORD_NOT_FOUND,
    RECORD_EXISTS,
    INVALID_KEYS,
    INSUFFICIENT_FUNDS,
    INVALID_ASSET,
    WALLET_DOES_NOT_EXIST,
}
